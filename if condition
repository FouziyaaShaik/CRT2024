#include<stdio.h>
int main() {
    int a,b;
    scanf("%d%d",&a,&b);
    if(b==0)
    {
        printf("second number can't be ZERO");
        b=1;
    }
    int r=a/b;
    printf("%d",r);

    return 0;
}
