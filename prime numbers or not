#include <stdio.h>

int main() {
    int n,c=0,i;
    scanf("%d",&n);
    for (i=2;i<n;i++)
    {
        if(n%i==0)
        {
            c++;
            break;
        }
    }
   if(c==0)
   printf("prime number");
   else
   printf("not a prime nummber");
    return 0;
}
